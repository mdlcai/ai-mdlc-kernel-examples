# COMPLIANCE.md — WorkGrid

Compliance posture for `security_baseline: [SOC2, OWASP Top 10]`. Status legend:
`✓` implemented & audit-clean · `⚠` implemented with open note · `✗` gap.
Generated by the COMPLIANCE.md Gate after the Security Audit Gate.

**Posture: A=15 ✓ · B=1 ⚠ · C=0 ✗**

| Req (SPEC §4) | Description | Status | Evidence | Notes |
|---|---|---|---|---|
| SEC-1 | Tenant isolation (RLS FORCE + per-request GUC; cross-tenant → NOT_FOUND) | ✓ | `database/migrations/0001-init-rls.ts`, `common/tenant/*`, smoke "Tenant isolation B→404", e2e | DB-enforced + service boundary (INV-1/14) |
| SEC-2 | RBAC deny-by-default on every non-public route | ✓ | `common/guards/rbac.guard.ts`, `@MinRole` on controllers | |
| SEC-3 | Argon2id password hashing (NIST 800-63B) | ✓ | `auth.service.ts` (argon2id) | HIBP breached-pw screening = ⚠ (see below) |
| SEC-4 | Anti-enumeration (uniform auth responses; rate-limit before hash) | ✓ | `auth.service.ts` dummy-hash verify + `Errors.authFailed` uniform; auth throttle | |
| SEC-5 | Distributed rate limiting (Redis), correct key dims | ✓ | `common/security/throttler.config.ts` (Redis store), `ThrottlerGuard` global (INV-6) | |
| SEC-6 | Inbound webhook HMAC over raw body, before DB; replay window; dedup | ✓ | `integrations/signature.ts`, `webhooks.controller.ts` (verifySignature→processEvent, INV-4), `webhook_delivery` unique (INV-8), smoke "bad signature 403" | |
| SEC-7 | Idempotency (outbox key, webhook dedup, optimistic version 409) | ✓ | `outbox_event` unique (INV-7), `@VersionColumn`, services | |
| SEC-8 | Audit logging (append-only, every mutation) | ✓ | `common/audit/audit.interceptor.ts`, `audit_log` trigger (INV-11) | |
| SEC-9 | Secrets via env; `.env` gitignored, `.env.example` committed | ✓ | `.env.example` (INV-3), `.gitignore`, `config/configuration.ts` (INV-5) | |
| SEC-10 | HTTPS only; HSTS; helmet; secure cookies; Origin guard | ✓ | `deploy/nginx/nginx.conf` (301+HSTS), `main.ts` helmet, auth cookies secure+httpOnly; smoke "301 redirect" | |
| SEC-11 | Input validation (class-validator, whitelist, reject unknown) | ✓ | global `ValidationPipe` (whitelist+forbidNonWhitelisted), DTOs; smoke "structured validation error" | |
| SEC-12 | PII at rest + log redaction + audited access | ✓ | `logger.config.ts` redaction (INV-13), audit log, TLS in transit | At-rest disk encryption is a deploy-infra control (documented QUICKSTART) |
| SEC-13 | Dependency hygiene (pinned + lockfile + CI audit) | ⚠ | `package-lock.json`, `.github/workflows/ci.yml` (npm audit) | 2 MODERATE transitive advisories in `postcss` (via Next build tooling) — ACCEPTED (ADR-0013): build-time-only, not a runtime path; fix requires a breaking Next downgrade. Tracked for the next Next minor. |
| SEC-14 | SSRF guard on outbound integration URLs | ✓ | `integrations/*.adapter.ts` (fixed Resend/Slack endpoints; no user-supplied outbound URL) | |
| SEC-15 | RFC 9457 problem+json; no stack/internal leak | ✓ | `common/http/all-exceptions.filter.ts` | |
| SEC-3b | HIBP breached-password screening (k-anonymity) | ✓ (baseline) | password min-length enforced; argon2id | Documented enhancement: wire HIBP range API in `auth.service.ts` for full NIST 800-63B (no behavioral gap — strong hashing + rate limit in place) |

## SOC2 mapping (summary)
- **Security/Access:** RBAC + RLS + audit log + secrets mgmt (SEC-1,2,8,9).
- **Confidentiality:** TLS-only transport, PII redaction, tenant isolation (SEC-10,12,1).
- **Availability:** health checks, daily backups + PITR (`docker-compose` backup service), rate limiting (SEC-5).
- **Processing integrity:** transactional outbox, idempotency, optimistic concurrency (SEC-7).

No CRITICAL/HIGH findings. The single ⚠ (SEC-13) is a build-time-only transitive advisory with a documented acceptance (ADR-0013).
