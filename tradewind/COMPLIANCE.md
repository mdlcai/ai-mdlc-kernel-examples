# COMPLIANCE.md — Tradewind

Compliance posture. Each SPEC.md §4 security/`compliance:` requirement mapped to implemented code +
status. Generated by the COMPLIANCE.md Gate (after Security Audit). Baselines: **PCI-DSS v4.0 (SAQ-A)**,
**SOC2**, **NIST SP 800-63B**.

Legend: ✓ implemented & audit-clean · ⚠ implemented with a noted limitation/deviation · ✗ gap.

| ID | Requirement | Status | Evidence | Notes |
|----|-------------|:------:|----------|-------|
| SEC-1 | Password hashing | ✓ | `auth.service.ts` hashPassword/verifyPassword (scrypt N=16384) | ADR-012: scrypt (memory-hard, OWASP) instead of bcrypt; equal/stronger. |
| SEC-2 | Breached-password screening (NIST SHALL) | ✓ | `auth.service.isBreachedPassword` (HIBP k-anonymity + offline corpus) | `auth.routes.test.ts` rejects `password123`. |
| SEC-3 | Length ≥15, ≤64, no composition, no rotation | ✓ | `auth.service.validatePasswordPolicy` | NIST 800-63B; stricter than PCI v4's 12. Tested. |
| SEC-4 | Rate-limit before hash compare | ✓ | `auth.service.login` calls `consume()` before `verifyPassword` | per-(ip,email) key. |
| SEC-5 | Anti-enumeration (shape + timing) | ✓ | `auth.service.register`/`login`; `auth.routes.test.ts` | identical register shape; generic login failure; dummy-hash timing parity. |
| SEC-6 | Session security | ✓ | `auth.routes.ts` cookieOpts (httpOnly+SameSite=Lax+Secure); `sessions` table | logout invalidates server-side. |
| SEC-7 | CSRF/Origin guard | ✓ | `middleware/csrf.ts` | Origin allow-list + SameSite=Lax primary defense; webhook exempt (HMAC). |
| SEC-8 | Tenant + object-level authz | ✓ | `middleware/authorize.assertResourceAccess`; `authz.test.ts` | INV-8; sub-resource IDOR closed (milestone ledger). |
| SEC-9 | Webhook signature before DB read | ✓ | `webhook.service.ts` (constructEvent first); `webhook.idempotency.test.ts` | INV-5 boundary-order; bad-sig → 400. |
| SEC-10 | Idempotent money mutations | ✓ | `ledger.service` UNIQUE idempotencyKey; Stripe `{idempotencyKey}` | INV-3; `ledger.service.test.ts`. |
| SEC-11 | No raw card data (PCI SAQ-A) | ✓ | hosted Checkout; no PAN columns (INV-9); secret scan clean | tokens only; card data bypasses Tradewind. |
| SEC-12 | At-rest encryption | ⚠ | ADR-008; QUICKSTART §Backups/Prod | **Delegated** to prod volume/RDS (KMS/TDE); dev unencrypted. Never marked ✓ per reconciliation rule. |
| SEC-13 | Audit logging | ✓ | `audit.service.ts`; `audit_logs` table | every fund-moving + admin action logged (SOC2). |
| SEC-14 | Rate limiting on public endpoints | ✓ | `middleware/rateLimit.ts`; auth routes | token bucket per dimension. |
| SEC-15 | Secrets in env only, zod-validated | ✓ | `config/env.ts`; `.env.example`; secret scan clean | fail-fast on missing required vars. |
| SEC-16 | Processing integrity (reconciliation) | ✓ | `reconciliation.service.ts`; worker; `/ledger/reconcile` | INV-7 + INV-12; Σ=0 verified in tests. SOC2. |
| SEC-17 | TLS in transit | ⚠ | ARCHITECTURE §6; QUICKSTART §Protocol & TLS | **Delegated** to reverse proxy in prod (HTTP→HTTPS + HSTS); local dev runs plaintext per documented dev exception. |

## Posture

- **A (✓ implemented & clean): 15**
- **B (⚠ implemented w/ noted limitation): 2** — SEC-12 (at-rest encryption delegated to infra),
  SEC-17 (TLS delegated to the reverse proxy; documented in QUICKSTART).
- **C (✗ gap): 0**

## Baseline summary (reconciled with detail rows)

- **PCI-DSS v4.0 (SAQ-A):** Card data never touches Tradewind (hosted Checkout, tokens only) → SAQ-A
  scope held (SEC-11 ✓). Password length floor met (SEC-3, 15 ≥ 12). **At-rest encryption: ⚠ delegated**
  (NOT "Applied" — honored per the summary↔detail reconciliation rule; SEC-12).
- **NIST SP 800-63B:** breach screening (SEC-2 ✓), length/no-composition/no-rotation (SEC-3 ✓), rate-limit
  before verifier (SEC-4 ✓) — verifier controls fully propagated.
- **SOC2:** audit logging (SEC-13 ✓), processing integrity via reconciliation (SEC-16 ✓), security
  controls across SEC-1…SEC-17.

Both ⚠ items are infrastructure-delegated controls (encryption-at-rest, TLS termination), documented in
QUICKSTART and DECISIONS (ADR-008), not implementation gaps. No ✗ gaps. The 2 deferred MEDIUM dependency
findings (SECURITY-AUDIT.md F-A3, build-time postcss) are tracked separately and do not map to a SPEC §4
control.
